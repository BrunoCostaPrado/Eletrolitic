[package]
name = "eletrolitic"
version = "0.1.0"
edition = "2024"

[lib]
name = "eletrolitic"
crate-type = ["rlib"]

[[bin]]
name = "eletrolitic"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true